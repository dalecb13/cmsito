# Copy to .env and set your database URL.
# For local dev with Docker: docker compose up -d then use the URL below.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tiny_cms?schema=public"
